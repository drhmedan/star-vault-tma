# Star Vault — البنية المعمارية وخطة التوسّع

> وثيقة مرجعية دائمة للفريق. توثّق القرارات الفنية، حدود الخدمات المجانية،
> القدرة الاستيعابية المحسوبة، ونموذج الربح ومسار التطوير المتبقي.
> آخر تحديث: 2026-09-10.

---

## 1. البنية الحالية (كما هي في الكود اليوم)

```
┌────────────────────────────── المتصفح (Telegram Mini App) ──────────────────────────────┐
│  React 19 + Vite (الواجهة)                                                              │
│  Three.js r186 (اللعبة ثلاثية الأبعاد)                                                  │
│  PeerJS Client ──────── WebRTC P2P (شبكة كاملة 2–8) ──────── PeerJS Client               │
│  TiDB Client (REST) ────── قراءة/كتابة الملف والاقتصاد ────── (قاعدة بيانات TiDB)        │
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

| المكوّن | الوضع الحالي | الملاحظة |
|---|---|---|
| الشبكة | WebRTC P2P عبر PeerJS (بدون سيرفر لعب) | شبكة كاملة (full mesh): كل لاعب يتصل بالآخرين عبر معرفات PeerJS حتمية — لا مرحّل، لا قنوات مكررة |
| عدد اللاعبين | حتى 8 مقاتلين: 2 بشر (1v1) + 6 بوتات | موصى به 2v2 للاعتمادية على الموبايل؛ البوتات تُحاكى محلياً |
| الإشارات | PeerJS ذاتية الاستضافة (`/peerjs`) | عبر سيرفر Koyeb المجاني |
| المطابقة | WebSocket `/match` | يجمع البشر في غرفة، يوزّع مقاعد حتمية (slot) لنقاط انتشار متناظرة، ويملأ الباقي ببوتات |
| قاعدة البيانات | TiDB (حسابات، مخزون، فواتير) | سليمة — لا تُلمس أثناء اللعب |
| الاقتصاد | دفتر أستاذ سيرفر-سايد (`/ledger/*`) | النجوم تُرهن وتُسوّى وتُمنح وتُشحن عبر السيرفر فقط؛ **بيع العناصر يدفع غبارًا لا نجومًا** (لا صكّ نجوم من المتصفح)؛ التخزين JSONL على قرص Koyeb، والترحيل إلى TiDB جاهز عبر `server/schema.sql` |

---

## 2. البنية المستهدفة (الترقية التدريجية)

```
                    ┌────────────────────────────────────────────┐
                    │  سيرفر Koyeb مجاني (512MB / 0.1 vCPU)      │
                    │  ────────────────────────────────────────   │
                    │  /match   → مطابقة WebSocket (طوابير → غرف) │
                    │  /peerjs  → إشارات WebRTC (استضافة ذاتية)   │
                    └──────────────┬─────────────────────────────┘
                                   │ إشارات + تكوين الغرف فقط
        ┌──────────────┬───────────┴──────────┬──────────────────┐
   ┌────▼────┐   ┌─────▼─────┐         ┌──────▼─────┐     ┌──────▼─────┐
   │ لاعب 1  │◄──┤ WebRTC P2P ├──► ...  │ لاعب 8     │     │ TiDB Starter│
   │ (host)  │   │ (حركة اللعب)│         │ (عميل)     │     │ (الثبات)    │
   └─────────┘   └────────────┘         └────────────┘     └────────────┘
```

**المبدأ**: سيرفر Koyeb يتعامل فقط مع **المطابقة + الإشارات** (رسائل JSON خفيفة).
حركة اللعب الفعلية تبقى **P2P بين الهواتف** — فلا يمر عبر السيرفر ولا عبر TiDB إطلاقاً.

---

## 3. الخدمات المجانية وحدودها (أرقام 2026)

| الدور | الخدمة | الحدود المجانية الفعلية |
|---|---|---|
| استضافة الواجهة | **Vercel** | استضافة ثابتة، ~100GB نقل شهرياً |
| سيرفر اللعب الحي | **Koyeb** | خدمة ويب واحدة: 512MB RAM / 0.1 vCPU / 2GB SSD، **دائماً مستيقظ (لا ينام)** |
| قاعدة البيانات | **TiDB Starter** | 5 GiB صفوف + 5 GiB أعمدة + **50 مليون Request Unit شهرياً** لكل كلاستر (حتى 5 كلاسترات مجاناً) |

**لماذا Koyeb دون غيره؟**
- Render/Railway **ينامان بعد 15 دقيقة** خمول (cold start يصل 47 ثانية) → قاتل لألعاب الزمن الحقيقي.
- Koyeb المجاني **دائم اليقظة** — يستجيب فوراً دون انتظار استيقاظ.

**لماذا الاستضافة الذاتية لـ PeerJS؟**
- PeerJS Cloud لم يعد يوفر طبقة مجانية موثوقة، وتجميد ~50 اتصالاً يومياً.
- الحل: نفس حاوية Koyeb تستضيف `peerjs-server` مجاناً وبلا حدود يومية.

---

## 4. القدرة الاستيعابية (الحساب الصادق)

### 4.1 سيرفر المطابقة (Koyeb 512MB / 0.1 vCPU)
| المقياس | التقدير المحافظ |
|---|---|
| اتصالات WebSocket متزامنة | **~500–1000** (الحد الفعلي هو 0.1 vCPU لا الذاكرة) |
| رسائل مطابقة/ثانية | آلاف (JSON صغير) |
| مباريات متزامنة (كل منها 2–8 لاعبين P2P) | **~100–200** |

> اللعب نفسه P2P — السيرفر لا يرى حزم الحركة، لذا لا يتأثر بعدد المباريات الجارية.

### 4.2 قاعدة البيانات (TiDB 50M RU شهرياً)
- قياس فعلي منشور: ~500 ألف استعلام استهلك 13M RU → أي **~7–8 مليون استعلام شهرياً مجاناً**.
- لعبتنا تكتب في القاعدة فقط عند: بدء مباراة، نهايتها، دفتر الرهانات، تحديث الملف (≈30 استعلاماً/مباراة).
- **النتيجة: مئات آلاف المباريات شهرياً مجاناً** — القاعدة ليست عنق زجاجة.
- 5 GiB ≈ **عشرات آلاف الحسابات** بمخزوناتها.

### 4.3 الخلاصة
| المقياس | قدرة المجاني |
|---|---|
| لاعبون متصلون لحظياً (لوبي + طابور) | ~500–1000 |
| حسابات مسجلة | عشرات الآلاف |
| مباريات شهرياً | مئات الآلاف |
| حجم المباراة الواحدة (P2P) | **8 لاعبين موصى به** (10 محفوف بالمخاطر على شبكات الموبايل) |

**الخلاصة**: المجاني يستوعب نمواً يفوق بكثير العام الأول. الأولوية للبناء الصحيح لا للقلق من السعة.

---

## 5. تصميم المطابقة وزمن التجميع

### 5.1 أوضاع اللعب (Game Modes — منجزة ✅)
| الوضع | بشر (حد أقصى) | إجمالي المقاتلين | نافذة التجميع | قواعد الحسم |
|---|---|---|---|---|
| **سريع (Quick)** | 2 (1v1) | 8 | 30 ث | آخر صامد · بوتات تملأ |
| **معركة حرة (FFA)** | 8 | 8 | 30 ث | كلٌّ لنفسه · آخر صامد · بوتات تملأ |
| **ثنائي ضد ثنائي (2v2)** | 4 | 4 | 45 ث | إقصاء الفريق المنافس · بوتات تملأ المقاعد |
| **فرق ٤ ضد ٤ (Squad)** | 8 | 8 | 60 ث | إقصاء الفريق المنافس · بوتات تملأ المقاعد |
| **صراع الفرق (TDM 4v4)** | 8 | 8 | 60 ث | أول فريق يبلغ 25 قتلة · إحياء فوري 5 ث · بوتات تملأ |
| **تنافسي (Ranked)** | 8 | 8 | 60 ث | بشر فقط · تجميع حسب MMR · كؤوس أعلى (±30/−18) |

قواعد الانطلاق الموحّدة: الغرفة تنطلق فور وجود **بشرَين أو أكثر**؛ اللاعب الوحيد ينتظر نافذة التجميع ثم يبدأ ضد بوتات (لا ينتظر أبداً). الفرق تُوزَّع بتعادل المقاعد (slot % 2) في السيرفر، فكل عميل يستنتج نفس الفرق والمواقع بدون مصافحة إضافية. أوضاع الفرق نوعان: **إقصاء الفريق (آخر فريق صامد)** في 2v2/Squad، و**إحياء فوري TDM** في tdm4v4 (هدف 25 قتلة للفريق، إعادة ظهور 5 ثوانٍ مع حماية عند الظهور، وبدون منطقة انكماش).

### 5.2 الوضع التنافسي (Ranked — بلا بوتات، تجميع MMR)
- **التقييم**: عدد الكؤوس (trophies) هو تصنيف اللاعب، يرسله العميل عند الدخول للطابور (`rating`).
- **التجميع**: يفضّل غرفة كاملة من **8 بشر** خلال الدقيقة الأولى؛ إن انقضت النافذة يتكوّن لوبي مصغّر (4–7) من **أقرب التقييمات** — تُختار النافذة الأضيق في قائمة الفرز (لا بوتات أبداً).
- **الحسم**: مباراة FFA (كلٌّ لنفسه، آخر صامد)؛ كؤوس أعلى من العادية (فوز +30 / خسارة −18) على السيرفر والعميل معاً.
- يتطلب خوادم المطابقة — بطاقة الوضع مقفلة في وضع التدريب المحلي.

### 5.3 بروتوكول المطابقة (WebSocket /match)
```
العميل → السيرفر: { type:'queue', payload:{ id, name, teamSize, mode, rating } }
                 { type:'leave_queue' }  { type:'ping' }
السيرفر → العميل: { type:'queue_status', payload:{ waiting, total } }
                 { type:'room_ready', payload:{ roomCode, mode, host, players[{id,name,slot,team}], fillBots, teamSize } }
                 { type:'pong' }
```

---

## 6. نموذج الربح (Monetization) — مرتّب بالأثر

الوضع الحالي: نجوم تيليجرام + صناديق + عجلة + معارك صناديق + غبار نجمي (أساس صحيح، ونجوم تيليجرام إجبارية على iOS داخل TMA).

| # | الإضافة | الأثر |
|---|---|---|
| 1 | **دفتر أستاذ سيرفر-سايد** (الرهانات والنتائج تُثبَّت عبر السيرفر) | ✅ **مُنجز** — JSONL على قرص Koyeb؛ الترحيل إلى TiDB بانتظار اعتماد الكلاستر |
| 2 | **Battle Pass موسمي** (مجاني + مدفوع 99 نجمة) | ✅ **مُنجز** — 30 مستوى/مسارين، شراء عبر `/ledger/purchase` ومنح النجوم عبر `/ledger/grant` |
| 3 | **سكنات** (ألوان جنود، كامو أسلحة، ألوان ذخائر ضوئية) | ✅ **مُنجز** — 12 سكنة إجرائية (زي جندي + طلاء سلاح + لون ذخيرة)، شراء عبر `/ledger/purchase` أو الغبار |
| 4 | **قطرات الصناديق** عند الفوز | ✅ **مُنجز** — غنيمة نصر مرجّحة (عنصر خزنة) عند كل فوز؛ العناصر تباع بالغبار فقط |
| 5 | **اشتراك VIP** (حقل `isVip` موجود): نجوم يومية + شارة ذهبية | ✅ **مُنجز** — شراء عبر `/ledger/purchase` ومطالبة يومية عبر `/ledger/grant` |

---

## 7. خارطة التطوير المتبقية (بالترتيب)

1. **العمود الفقري للمالتي بلاير v2** ← *(يُبنى الآن)*
   - سيرفر مطابقة + إشارات (server/) ✅
   - شاشة تجميع «انضم X/8» في اللوبي + عدّاد
   - ملء البوتات + ربط اللوبي بالسيرفر
2. **اقتصاد آمن سيرفر-سايد** ✅ *(مُنجز — دفتر `/ledger/*`، JSONL الآن + ترحيل TiDB لاحقاً)*
3. **توسيع الربح** (Battle Pass ✅ + سكنات ✅ + VIP ✅ + قطرات النصر ✅)
4. **أوضاع لعب** ✅ *(مُنجز — FFA حتى 8، ثنائي 2v2، فرق 4v4، وعي الفرق + حسم الفريق + بوتات تملأ + نقاط إنزال فرق)*
5. **الاحتفاظ** ✅ *(مُنجز — مهام يومية متناوبة، إنجازات دائمة، رتب/MMR فوق الكؤوس، لوحة صدارة سيرفر-سايد)*
   - مهام يومية: 4 مهام يومياً من تجمع 9 (تناوب حتمي بالتاريخ)، تقدم محلي، مكافآت نجوم عبر `/ledger/grant` + غبار نجمي
   - إنجازات: 14 إنجازاً دائماً فوق إحصائيات المسيرة (قتل/فوز/مباريات/ضرر/رؤوس/أفضل كؤوس) مع غنائم
   - الرتب: 7 رتب (برونزي→أسطورة) فوق كؤوس التقييم، تُعرض في الرأس واللوبي وشاشة الرتب
   - الصدارة: `GET /ledger/leaderboard` حقيقية بالكامل (اسم اللاعب يُلتقط عند التسوية)
6. **اجتماعي / تنافسي** ✅ *(مُنجز — كاميرا القتل، متفرج الكاميرا المتبعة، TDM 4v4 بإعادة الظهور)*
   - **كاميرا القتل**: عند السقوط في أوضاع الإقصاء تُعرض لقطة سينمائية (3.2 ث) تُؤطّر القاتل وتُسمّيه، ثم تُحسم النتيجة (هزيمة فردية / متابعة الفريق).
   - **متفرج المتابعة**: في أوضاع الفرق بعد السقوط، الكاميرا تركب فوق كتف زميل حيّ (فريقك ما زال يقاتل) بدلاً من البقاء على الجثة.
   - **TDM 4v4 بإعادة الظهور**: وضع فريق كامل — 8 مقاتلين، أول فريق يبلغ 25 قتلة يفوز (أو الأكثر عند انتهاء الوقت)؛ إحياء فوري بعد 5 ثوانٍ عند نقطة الفريق مع حماية ظهور 2.5 ثانية، والمنطقة لا تنكمش.
7. **تنافسي مُصنّف (Ranked MMR)** ✅ *(مُنجز — تجميع حسب التقييم، لوبيات بشرية فقط، كؤوس أعلى ±30/−18)*
   - التقييم = كؤوس اللاعب؛ السيرفر يجمع الطابور في أضيق نافذة تقييم (8 بشر خلال دقيقة، ثم 4+ من الأقرب).
   - بطاقة «التنافسي» في اللوبي تُظهر الرتبة، وتُقفل في وضع التدريب المحلي.
8. **مستقبلاً** ← *(ملاحظات الموت المرئية، أطوار/فرق مخصّصة، TURN للشبكات الصارمة)*

---

## 8. محفّزات الترقية (متى ندفع)

| الإشارة | الترقية | التكلفة التقريبية |
|---|---|---|
| استمرار CPU في Koyeb فوق ~70% | Eco instance | من **$1.61/شهر** |
| تجاوز 50M RU في TiDB | الدفع حسب الاستخدام | **$0.10 / مليون RU** |
| تجاوز 100GB نقل في Vercel | Hobby | ~$20/شهر |
| الحاجة لـ 10 لاعبين/مباراة بثبات | سيرفر لعب مركزي (وليس P2P) | قرار معماري لاحق |

---

## 9. نشر سيرفر المطابقة على Koyeb

1. ادفع `server/` إلى المستودع (هذا الملف).
2. في Koyeb: **Create Service → Dockerfile** من المسار `server/Dockerfile`.
3. حدد المنطقة `Frankfurt` (المجانية) واترك الحجم الافتراضي المجاني.
4. المنفذ: `8000` (متغير البيئة `PORT` اختياري).
5. حدّث ثوابت الاتصال في واجهة اللعبة: عنوان السيرفر + مسار `/peerjs`.

---

## 10. مخاطر وتخفيفات

| الخطر | التخفيف |
|---|---|
| NAT صارم (10–15% من الشبكات) يمنع اتصال P2P | إضافة TURN مستقبلاً + الفلّباك لبوتات في تلك الحالة |
| انسحاب الـ host يُفقد المباراة | ترحيل الـ host للاعب التالي (في خطة v2) |
| 0.1 vCPU يختنق تحت ضغط مفاجئ | مراقبة `/health` + ترقية Eco عند 70% CPU |
| غش الاقتصاد | دفتر أستاذ سيرفر-سايد (البند 2 في الخارطة) |
